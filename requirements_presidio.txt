# Requirements for running Presidio (PII detection and anonymization)
# Install with: pip install -r requirements_presidio.txt
# After installation, download spaCy models:
#   python -m spacy download en_core_web_lg
#   python -m spacy download en_core_web_sm
#   python -m spacy download de_core_news_lg  # For German support

# Core Presidio packages
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# NLP backends
spacy>=3.0.0
flair>=0.12.0

# OpenAI for synthetic data generation
openai>=1.0.0

# Environment and utilities
python-dotenv>=1.0.0

# Database (needed for imports, but not used on server)
psycopg2-binary>=2.9.0

# Required: Streamlit for caching decorators
streamlit>=1.30.0
