# Requirements for Differential Privacy (DP) processing on server
# Used by: Diffractor, DPMLM, PrivFill (DP.py imports all three)
# Install with: pip install -r requirements_dp.txt
# After install: python -m nltk.download punkt
# After install: python -m nltk.download punkt_tab
# After install: python -m nltk.download stopwords

# --- Diffractor (1-Diffractor) ---
diffprivlib>=0.6.0
faiss_cpu>=1.7.4
gensim>=4.3.0
importlib_resources>=6.1.1
nltk>=3.8.1
numpy>=1.24.0
scipy>=1.11.0,<1.12
tqdm>=4.66.0

# --- DPMLM (DP.py imports it) ---
spacy>=3.0.0
torch>=1.9.0
transformers>=4.0.0
wn

# --- PrivFill / DPPrompt (DP.py imports it) ---
mpmath
pandas>=1.0.0

# --- Shared / utilities ---
python-dotenv>=1.0.0

# Required by Data_loader (Database_management import; not used on server)
psycopg2-binary>=2.9.0
